import { TopRoundPipe } from './top-round.pipe';

describe('TopRoundPipe', () => {
  it('create an instance', () => {
    const pipe = new TopRoundPipe();
    expect(pipe).toBeTruthy();
  });
});
